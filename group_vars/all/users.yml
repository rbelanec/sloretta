---
# ── Cluster users ───────────────────────────────────────────────────────────
# Add/remove users here and re-run the playbook to provision them on all nodes.
# UIDs are assigned automatically by Linux. All nodes must be provisioned
# together to ensure consistent UIDs for NFS home directory ownership.
cluster_users:
  - robert
